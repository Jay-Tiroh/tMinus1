import React from "react";
import { StyleSheet, Text, View } from "react-native";

const TradeDetails = () => {
  return (
    <View style={styles.container}>
      <Text>TradeDetails</Text>
    </View>
  );
};

export default TradeDetails;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
